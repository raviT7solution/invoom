overwrite: true
schema: ${SCHEMA_PATH}
documents: "src/api/base.graphql"
generates:
  src/api/base.ts:
    config:
      skipTypename: true
    plugins:
      - add:
          content: |
            /* eslint-disable */
      - typescript
      - typescript-operations
      - typed-document-node
