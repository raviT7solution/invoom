overwrite: true
schema: "../schema.graphql"
documents: "src/api/base.graphql"
generates:
  src/api/base.ts:
    config:
      disableDescriptions: true
      enumsAsTypes: true
      skipTypename: true
    plugins:
      - add:
          content: |
            /* eslint-disable */
      - typescript
      - typescript-operations
      - typed-document-node
